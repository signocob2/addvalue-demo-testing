database:
  datasource:
    jdbcUrl: jdbc:h2:mem:TESTAUTOMATICI;MODE=DB2;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;DATABASE_TO_UPPER=true;INIT=CREATE SCHEMA IF NOT EXISTS TESTAUTOMATICI
    username: sa
    password:
    driver-class-name: org.h2.Driver
    schema: TESTAUTOMATICI

spring:
  batch:
    job:
      enabled: false
